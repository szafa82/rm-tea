scripts:
  buildCommand: npm install && npm run build
  runCommand: npm install --omit=dev && npm run start

outputFiles:
  serverApp:
    include:
      - dist
      - server.js
      - package.json
